info:
  name: Listado de productos
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/{{prefix}}/products"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
